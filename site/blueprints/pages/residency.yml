title: Residency

columns:
  main_1:
    width: 2/3
    sections:
      pages:
        headline: Residents
        type: pages
        image: icon
        templates:
          - resident
      main_fields:
        type: fields
        fields:
          shelf:
            label: Shelf
            type: pages
            query: page.children
            text: "{{ page.title }}"
            empty: No related posts found
            sortBy: date desc
            layout: cards
            size: small
            info: "{{ page.date }}"
  main_2:
    width: 1/3
    sections:
      fields:
        fields:
          main_content:
            label: Header
            type: textarea
            size: tiny
          main_description:
            label: Description
            type: textarea
            size: large
      files: