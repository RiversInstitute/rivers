title: Artists

columns:
  main_1:
    width: 2/3
    sections:
      pages:
        headline: Artists
        type: pages
        templates:
          - artist
      # main_fields:
      #   type: fields
      #   fields:
      #     shelf:
      #       label: Shelf
      #       type: pages
      #       query: page.children
      #       text: "{{ page.title }}"
      #       empty: No related posts found
      #       sortBy: date desc
      #       layout: cards
      #       size: small
      #       info: "{{ page.date }}"
  main_2:
    width: 1/3
    sections:
      fields:
        fields:
          # main_content:
          #   label: Header
          #   type: textarea
          #   size: tiny
          # blurb:
          #   label: Blurb
          #   type: textarea
          #   size: small
          # main_description:
          #   label: Description
          #   type: textarea
          #   size: large
          color:
            label: Color
            type: color
            default: "#ff6253"
      files: